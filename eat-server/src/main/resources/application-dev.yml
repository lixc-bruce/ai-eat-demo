# 开发环境配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/eat_db?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai
    username: root
    password: root
  data:
    redis:
      host: localhost
      port: 6379
      password:

eat:
  jwt:
    secret: dev-secret-key-at-least-256-bits-long-for-hmac-sha256-algorithm-use-in-development
    expiration: 604800000

logging:
  level:
    com.eat: debug
